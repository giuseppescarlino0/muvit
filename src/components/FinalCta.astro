<section class="final-cta" id="contatti">
    <div class="container">
      <div class="final-cta-card">
        <div class="final-cta-content">
          <p class="final-cta-kicker">Prenota ora</p>
          <h2>Pronto a partire con MUVIT?</h2>
          <p>
            Scegli il veicolo più adatto alle tue esigenze e completa la prenotazione
            in pochi passaggi, direttamente online.
          </p>
        </div>
  
        <div class="final-cta-action">
          <a href="#booking" class="final-cta-button">Prenota adesso</a>
        </div>
      </div>
    </div>
  </section>
  
  <style>
    .final-cta {
      padding: 88px 0 110px;
      background: linear-gradient(180deg, #f4ede7 0%, #efe5dc 100%);
    }
  
    .container {
      width: min(1200px, calc(100% - 32px));
      margin: 0 auto;
    }
  
    .final-cta-card {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 32px;
      padding: 40px;
      border-radius: 32px;
      background:
        radial-gradient(circle at top left, rgba(233, 85, 25, 0.14), transparent 30%),
        rgba(255, 255, 255, 0.78);
      border: 1px solid rgba(255, 255, 255, 0.82);
      backdrop-filter: blur(14px);
      box-shadow: 0 24px 60px rgba(31, 20, 14, 0.1);
    }
  
    .final-cta-content {
      max-width: 700px;
    }
  
    .final-cta-kicker {
      margin: 0 0 14px;
      font-size: 0.82rem;
      font-weight: 800;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: #e95519;
    }
  
    .final-cta-content h2 {
      margin: 0;
      font-size: clamp(2.3rem, 4vw, 4rem);
      line-height: 0.96;
      letter-spacing: -0.05em;
      color: #1a1a1a;
    }
  
    .final-cta-content p:last-child {
      margin: 18px 0 0;
      max-width: 58ch;
      font-size: 1.04rem;
      line-height: 1.75;
      color: #5f5a56;
    }
  
    .final-cta-action {
      flex-shrink: 0;
    }
  
    .final-cta-button {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-height: 56px;
      padding: 0 28px;
      border-radius: 999px;
      background: #e95519;
      color: #fff;
      font-weight: 700;
      text-decoration: none;
      white-space: nowrap;
      box-shadow: 0 14px 30px rgba(233, 85, 25, 0.22);
      transition: transform 0.2s ease, background 0.2s ease;
    }
  
    .final-cta-button:hover {
      transform: translateY(-1px);
      background: #cf4912;
    }
  
    @media (max-width: 900px) {
      .final-cta-card {
        flex-direction: column;
        align-items: flex-start;
        padding: 28px;
        border-radius: 24px;
      }
    }
  
    @media (max-width: 768px) {
      .final-cta {
        padding: 64px 0 84px;
      }
  
      .final-cta-content h2 {
        font-size: clamp(2rem, 9vw, 3rem);
      }
  
      .final-cta-button {
        width: 100%;
      }
  
      .final-cta-action {
        width: 100%;
      }
    }
  </style>